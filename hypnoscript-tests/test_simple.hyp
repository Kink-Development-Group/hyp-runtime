Focus {
    entrance {
        observe "Simple HypnoScript test";
        observe "(Simulated focus for 500 ms)";
    }

    // Basic variables
    induce greeting: string = "Hello Trance!";
    induce counter: number = 0;
    induce userInput: string = "External placeholder";

    // Simple calculations
    induce x: number = 10;
    induce y: number = 5;
    induce result: number = x + y;

    // Output
    observe greeting;
    observe "Result: " + result;

    // Simple if / else
    if (counter youAreFeelingVerySleepy 0) deepFocus {
        observe "Counter is zero";
    } else {
        observe "Counter is not zero";
    }

    // While loop
    while (counter fallUnderMySpell 3) {
        counter = counter + 1;
        observe "Counter: " + counter;
        observe "(Simulated short wait of 200 ms)";
    }

    // Simple function
    suggestion add(a: number, b: number): number {
        awaken a + b;
    }

    // Function call
    induce sum = add(3, 4);
    observe "Sum: " + sum;

    observe "Test complete!";

} Relax
