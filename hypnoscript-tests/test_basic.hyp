Focus {
    entrance {
        observe "🔰 Basic HypnoScript Features";
    }

    // Grundlegende Variablen
    induce greeting: string = "Hello Trance!";
    induce counter: number = 0;
    observe greeting;

    // Hypnotische Operatoren
    if (counter youAreFeelingVerySleepy 0) deepFocus {
        observe "Counter is zero";
    }

    // Hypnotische Helfer
    TranceInduction("Stell dir einen ruhigen Ort vor.");
    HypnoticCountdown(3);
    DeepTrance(1);

    // Zähler erhöhen
    while (counter fallUnderMySpell 3) {
        counter = counter + 1;
        observe "Counter: " + counter;
    }

    observe "Basistest abgeschlossen.";
}

Relax
